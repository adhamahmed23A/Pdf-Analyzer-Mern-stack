import { Spinner } from "./ui/spinner";

export const AppSpinner = () => (
  <div className="flex items-center justify-center h-screen">
    <Spinner className="w-10 h-10  text-brand-color" />
  </div>
);
